name: gpu
image: ghcr.io/mihneateodorstoica/dsml-kit:full
description: Full workspace with GPU runtime access enabled.
gpu: true
image_build:
  args:
    PYTHON_VERSION: "3.11"
    DSML_REQUIREMENTS: requirements-full.txt
